# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BindoLabTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'RealmSwift'
	pod 'ObjectMapper', '~> 3.3'
	pod 'RxSwift', '~> 4.0'
	pod 'RxCocoa', '~> 4.0'
	pod 'RxRealm'
	pod 'Kingfisher', '~> 4.0'

  # Pods for BindoLabTest

end
